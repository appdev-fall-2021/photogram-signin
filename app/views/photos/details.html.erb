<h1>Item Details</h1>

<dl>
  <dt>Item image</dt>
  <dd>
    <img src="<%= @photo.image %>">
  </dd>

  <dt>Description</dt>
  <dd>
    <p><%= @photo.caption %></p>
  </dd>

  <dt>Seller</dt>
  <dd>
    <%= @photo.poster.username %>
  </dd>

  <dt>Posted</dt>
  <dd>
    2 months ago
  </dd>

</dl>

<h2>Who's interested?</h2>

<table>
  <tr>
    <th>Buyer</th>
    <th>Phone number here</th>
    <th>Posted</th>
  </tr>

  <% @photo.comments.each do |comment| %>
  <tr>
    <td>
      <%= comment.commenter.username %>
    </td>

    <td>
      <%= comment.body %>
    </td>

    <td>
      <%= comment.created_at %>
    </td>
  </tr>
  
  <% end %>
</table>

<h3>Interested? Share your phone number here:</h3>

<form action="/insert_comment_record" method="post">
  <input id="browser_photo_id" type="hidden" value="<%= @photo.id %>" name="input_photo_id">

  <label for="browser_comment">Phone number</label>
  <textarea id="browser_comment" name="input_body" placeholder="Enter your phone number..."></textarea>

  <button>Submit number!</button>
</form>
